# Log — July 2026

## 2026-07-02 — Scaffold (bootstrap session)

- Repo scaffolded: docs (ARCHITECTURE/STYLE/WORKFLOW/LESSONS/PLAN), agent
  personas (mason, arbiter, oracle, warden, cartographer, chronicler),
  opencode config + commands, schedule scripts.
- Specs downloaded to `docs/specs/html` (xmlschema11-1, xmlschema11-2,
  xpath20, xsd-precisionDecimal) and converted to Markdown via
  `tools/spec2md`.
- W3C test suite added as submodule at `testdata/xsdtests` (pinned).
- Foundations implemented: `xsderr` (rule-mapped, located, decorated
  errors), `loader` (Resolver + file/http/map/chain helpers), `conformance`
  ratchet mechanics (expectations parse/compare/upward-rebaseline) with
  unit tests. Contract-only `doc.go` for the rest.
- Decisions of record: Ollama-served Gemma 4; submodule (not vendored)
  suite; XPath grown from subset, fail-open; module
  `github.com/kud360/goxsd7`; value backends user-pluggable per type with
  fallback composition (`value.Override`).
- Next: cartographer to open M1/M2 issues (`/plan`), then `/develop`.
- Late additions (same session, second pass):
  - xs:choice in generated code = sealed interface + type switch; T2
    amended with the closed-sum exception (warden checklist updated to
    match — sealed sums are demanded, capability erosion still rejected).
  - codec built for minimal allocation: runtime appender APIs
    (ParseBytes/AppendCanonical) + generated fast path where backends
    export their own emitters (`value.Emitter`, API to be fixed in M7).
  - Correctness contract between the two decode paths = differential
    testing (identical values, identical error rule IDs); debuggability =
    stage-tagged decode errors + GOXSD_DEBUG=codec tracing. PLAN M7/M8
    acceptance updated accordingly.
  - Lint gate added (.golangci.yml, golangci-lint v2, runs in `make
    vet`): machine-checkable STYLE subset only — errcheck/errorlint/
    nilerr, revive early-return/superfluous-else/indent-error-flow,
    exhaustive, sloglint no-global, forbidigo (io.ReadAll, fmt.Print*),
    baseline staticcheck set. Judgment rules (T4/D2/D3/E2) deliberately
    left to arbiter+warden. First run found 6 real issues (unchecked
    Closes, non-exhaustive Ordering switch); fixed per S3.
  - Codegen namer: single component owns XSD → Go identifier decisions;
    anonymous types named from schema ancestor context (nearest named
    ancestors, path lengthened only as needed), collisions disambiguated
    deterministically by document order.

- Schedule went live: `aggressive` preset installed via launchd on the
  maintainer's machine (develop 30m / plan 6h / ratchet 22:00 / retro
  09:00 daily). agent-loop now waits up to GOXSD_LOCK_WAIT (default
  900s) for the session lock instead of skipping, so plan/retro aren't
  starved by the dense develop cadence.

- Tried and REJECTED: opencode-scheduler plugin with a single "pulse"
  job dispatching triggers via a script. Rejected by the maintainer;
  friction observed during the trial: job management goes through LLM
  tool calls (`opencode run "use schedule_job..."`) instead of a
  deterministic CLI, and the install session ran under a global default
  agent/model (orchestrator · gpt-5.5) rather than the project's local
  model — scheduling machinery must not depend on model behavior or risk
  leaking to paid APIs. Direct launchd (preset command) is the standard;
  do not re-propose the plugin without new evidence.
- GitHub planning surface bootstrapped: labels (ready/blocked/
  needs-replan/epic, area/*, kind/*) and milestones M1–M9 created.
  Aggressive launchd preset re-installed and live.

### Process notes

- (chronicler) Keep entries in this format: what happened, decisions,
  surprises, next. Newest entry at the bottom. One file per month.
